[logstool]
* LoggerClient with qlog==None, check qlog and skip queue if not exists

[base_th]
* tests for classes

[base_logs]
* test for class LoggerQueue, BLogFormatter

[systemtool]->[CommandLineParser]
* Refactoring: 
    - configure_argument -> configure_option
    - parse_arguments -> parse
    - have_option
* add deprecation info
